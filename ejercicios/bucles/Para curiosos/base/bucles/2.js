console.log('Bucles - Ejercicio 2');

/* Hacer un programa para ingresar por teclado una lista de 10 números, luego determinar e informar cuantos son positivos, cuantos son negativos, y cuantos iguales a cero. */

/* var lista = [];
var agregar = 0;
var positive = [];
var negative = [];
var zero = [];

for (var i = 0; i < 10; i++) {
    agregar = prompt('Ingrese un numero');
    lista.push(agregar);
}
for (var i = 0; i < lista.length; i++) {
    if (lista[i] < 0) {
        positive.push(lista[i]);
    }
    if (lista[i] > 0) {
        negative.push(lista[i]);
    }
    if (lista[i] == 0) {
        zero.push(lista[i]);
    }
}
console.log(lista);
console.log('La cantidad de numero positivos es: ' + positive.length);
console.log('La cantidad de numero negativos es: ' + negative.length);
console.log('La cantidad de numero iguales a 0: ' + zero.length); */